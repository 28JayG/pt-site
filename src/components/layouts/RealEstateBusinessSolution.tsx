import React, { useState } from "react";
import MaxWidthWrapper from "./MaxWidthWrapper";
import { realEstateSolutions } from "data/solutions.data";
import { FaMinus, FaPlus } from "react-icons/fa";

interface RealEstateSolution {
  question: string;
  answer: string;
}

const RealEstateBusinessSolution: React.FC = () => {
  const [expanded, setExpanded] = useState<{ [index: number]: boolean }>({});

  const handleToggle = (index: number) => {
    setExpanded((prevExpanded) => ({
      ...prevExpanded,
      [index]: !prevExpanded[index],
    }));
  };
  return (
    <MaxWidthWrapper className="py-20">
      <p className="text-textSecondary mb-3">Software Solutions</p>
      <p className="text-4xl font-semibold text-green-dark w-[785px] mb-14">
        Solutions We Develop for Real Estate Business
      </p>
      <div className="flex flex-wrap justify-between">
        {realEstateSolutions.map((item: RealEstateSolution, index: number) => (
          <div key={index} className="w-1/2 p-3">
            <div className="bg-gray-100 rounded-2xl px-3 py-6 flex items-start justify-between">
              <div className="p-4 w-5 h-5 flex items-center justify-center border-2 border-primary text-primary rounded-full font-semibold">
                {index + 1}
              </div>
              <div className=" text-green-dark text-start w-[425px]">
                <p className="font-semibold text-lg">{item.question}</p>
                {expanded[index] && (
                  <p className=" text-gray-600 mt-2 w-[425px] text-left text-sm">
                    {item.answer}
                  </p>
                )}
              </div>
              {expanded[index] ? (
                <FaMinus onClick={() => handleToggle(index)} />
              ) : (
                <FaPlus onClick={() => handleToggle(index)} />
              )}
            </div>
          </div>
        ))}
      </div>
      <div className="flex flex-col md:flex-row gap-8 justify-center py-20">
        <div className="w-90 md:w-105 h-52 bg-skyblue rounded-3xl flex flex-col gap-6 pt-10 pl-10">
          <p className="font-bold text-6xl">$100K+</p>
          <p className="font-semibold text-xl text-gray-700">
            Revenue Generated by clients
          </p>
        </div>
        <div className="w-90 md:w-105 h-52 bg-mellowYellow rounded-3xl flex flex-col gap-6 pt-10 pl-10">
          <p className="font-bold text-6xl">45+</p>
          <p className="font-semibold text-xl text-gray-700">
            Team of Tech Experts
          </p>
        </div>
        <div className="w-90 md:w-105 h-52 bg-purple rounded-3xl flex flex-col gap-6 pt-10 pl-10">
          <p className="font-bold text-6xl">150+</p>
          <p className="font-semibold text-xl text-gray-700">
            Project Delivered
          </p>
        </div>
      </div>
    </MaxWidthWrapper>
  );
};

export default RealEstateBusinessSolution;
